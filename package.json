{
  "name": "fridgeai-backend",
  "version": "1.0.0",
  "description": "FridgeAI API proxy — keeps the Anthropic key secret",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "jose": "^6.2.10"
  }
}
